<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ═══ Permissions ════════════════════════════════════════════════ -->

    <!-- Storage — needed for ROM scanning and PlayFieldPortal/ folder -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- API 33+ scoped storage — declared in runtime permission request -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- ROM libraries, media libraries and backups all use SAF (folder picker) and need no
         storage permission, so MANAGE_EXTERNAL_STORAGE (all-files access) is no longer declared. -->


    <!-- Launcher role — see all installed apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- App Drawer uninstall action — lets the system uninstaller accept our ACTION_DELETE intent
         (normal permission, auto-granted; the OS still shows its own confirmation). -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <!-- Internet — SteamGridDB, emulator profile updates -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Restore state on boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- WorkManager background tasks -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Capture legacy game shortcuts from apps that still use the old broadcast
         (BannerHub, older Winlator). The modern path is LauncherApps shortcut harvesting. -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <!-- Playtime tracking -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Controller input -->
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <application
        android:name=".PFPApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.PFP"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:allowBackup="false">

        <!-- ═══ Launcher Activity ═══════════════════════════════════════ -->
        <!-- screenOrientation="user": Android's own auto-rotate decides, and the user's rotation
             lock is honoured. NOT "sensor", which ignores the lock — a device held sideways would
             spin the launcher while every other app on it stayed put. It was sensorLandscape,
             which pinned the launcher regardless of the system setting. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="user"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|navigation"
            android:windowSoftInputMode="stateAlwaysHidden">

            <!-- HOME intent — this makes PFP a launcher -->
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Standard launcher entry point -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ═══ Pinned-shortcut confirmation ═══════════════════════════════ -->
        <!-- Declaring this makes PFP advertise pin support so apps using the modern
             requestPinShortcut() (BannerHub, modern Winlator) can create game shortcuts.
             Invisible: it accepts the pin, records the entry, and finishes. -->
        <activity
            android:name=".pin.PinShortcutActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.content.pm.action.CONFIRM_PIN_SHORTCUT" />
            </intent-filter>
        </activity>

        <!-- ═══ Legacy INSTALL_SHORTCUT capture (BannerHub, old Winlator) ═══ -->
        <!-- Best-effort manifest registration. Android 8+ blocks this implicit broadcast for
             background manifest receivers, so MainActivity also registers it at runtime. -->
        <receiver
            android:name=".receiver.InstallShortcutReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.launcher.action.INSTALL_SHORTCUT" />
            </intent-filter>
        </receiver>

        <!-- Handles the Add/Ignore decision for a captured shortcut. NOT exported and has no
             intent-filter, so only PFP's own notification PendingIntents can trigger it — the
             sending app cannot forge a confirmation. -->
        <receiver
            android:name=".receiver.ShortcutConfirmReceiver"
            android:exported="false" />

        <!-- ═══ WorkManager ══════════════════════════════════════════════ -->
        <!-- On-demand init: PFPApplication is the Configuration.Provider (Hilt-Work), so the -->
        <!-- default WorkManagerInitializer must be removed from the startup provider.        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <!-- ═══ Music playback foreground service ══════════════════════ -->
        <!-- Keeps in-app music playing (with a media notification) while the user is in other
             apps. Started on-demand from the player's "Play in Background". -->
        <service
            android:name="com.psplauncher.feature.xmb.music.MusicPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- ═══ Notification access — the device's own notifications in the strip ═══
             Exported and bound by the system, which is what this permission means. It cannot be
             granted by a dialog: the user switches the launcher on in
             Settings ▸ Notification access, and until they do the service is never constructed
             and the launcher shows no count. -->
        <service
            android:name="com.psplauncher.launcher.notification.PfpNotificationListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ═══ FileProvider — ROM URIs passed to emulators ════════════ -->
        <!-- EmulatorIntentResolver uses this to build content:// URIs    -->
        <!-- that emulators can read without MANAGE_EXTERNAL_STORAGE       -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
