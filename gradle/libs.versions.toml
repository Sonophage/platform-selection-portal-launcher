[versions]
agp                     = "9.4.0"
benchmark               = "1.5.0"
kotlin                  = "2.4.10"
junit                   = "4.13.2"
mockk                   = "1.14.11"
turbine                 = "1.2.1"
robolectric             = "4.16.1"
androidx-test-core      = "1.7.0"
hilt-testing            = "2.60.1"
ksp                     = "2.3.11"
compose-bom             = "2026.08.00"
hilt                    = "2.60.1"
room                    = "2.8.4"
datastore               = "1.2.1"
navigation              = "2.10.0"
lifecycle               = "2.11.0"
coil                    = "3.6.1"
ktor                    = "3.5.2"
retrofit                = "3.0.0"
timber                  = "5.0.1"
xz                      = "1.12"
workmanager             = "2.11.2"
coroutines              = "1.11.0"
serialization           = "1.11.0"
core-ktx                = "1.19.0"
activity-compose        = "1.13.0"
splashscreen            = "1.2.0"
accompanist             = "0.37.3"
media3                  = "1.11.0"
# OkHttp is never declared directly: it arrives transitively through Coil's network fetcher,
# Retrofit (Steam) and ktor-client-okhttp, which resolves it to 5.3.2. The version is pinned here
# only so every OkHttp artifact moves with the rest of the stack instead of an older one calling
# 5.x internals it was not built against.
okhttp                          = "5.3.2"
browser                 = "1.10.0"
# Frozen artifact: the Compose BOM stopped managing it and upstream ships no further updates.
# Successor is Material Symbols vector resources; pinned explicitly until that migration happens.
material-icons          = "1.7.8"
# Compose Multiplatform for the desktop Theme Studio. Its Compose compiler comes from the
# org.jetbrains.kotlin.plugin.compose compiler plugin, so it tracks the Kotlin version above.
compose-multiplatform   = "1.12.0"
compose-mp-material3    = "1.9.0"
# Same story as material-icons above: frozen upstream, no releases past 1.7.3.
compose-mp-icons        = "1.7.3"
# Pure-Java MP4/H.264 reader for the Theme Studio's video -> GIF motion-wallpaper import.
# Deliberately NOT ffmpeg/JavaCV: :studio must stay pure JVM (no natives in the jpackage
# installer), and a pure-Java parser cannot be exploited the way a native decoder can.
jcodec                  = "0.2.5"

[libraries]
# AndroidX Core
androidx-core-ktx               = { group = "androidx.core",         name = "core-ktx",                   version.ref = "core-ktx" }
androidx-activity-compose       = { group = "androidx.activity",      name = "activity-compose",            version.ref = "activity-compose" }
androidx-splashscreen           = { group = "androidx.core",          name = "core-splashscreen",           version.ref = "splashscreen" }
androidx-documentfile           = { group = "androidx.documentfile",  name = "documentfile",                version = "1.1.0" }
media3-exoplayer                = { group = "androidx.media3",         name = "media3-exoplayer",            version.ref = "media3" }
media3-ui                       = { group = "androidx.media3",         name = "media3-ui",                   version.ref = "media3" }
media3-transformer              = { group = "androidx.media3",         name = "media3-transformer",          version.ref = "media3" }
media3-effect                   = { group = "androidx.media3",         name = "media3-effect",               version.ref = "media3" }
media3-common                   = { group = "androidx.media3",         name = "media3-common",               version.ref = "media3" }

# Compose BOM
compose-bom                     = { group = "androidx.compose",       name = "compose-bom",                 version.ref = "compose-bom" }
compose-ui                      = { group = "androidx.compose.ui",    name = "ui" }
compose-ui-tooling-preview      = { group = "androidx.compose.ui",    name = "ui-tooling-preview" }
compose-ui-tooling              = { group = "androidx.compose.ui",    name = "ui-tooling" }
compose-ui-test-junit4          = { group = "androidx.compose.ui",    name = "ui-test-junit4" }
compose-ui-test-manifest        = { group = "androidx.compose.ui",    name = "ui-test-manifest" }
compose-material3               = { group = "androidx.compose.material3", name = "material3" }
compose-animation               = { group = "androidx.compose.animation", name = "animation" }
compose-foundation              = { group = "androidx.compose.foundation", name = "foundation" }
compose-graphics                = { group = "androidx.compose.ui",    name = "ui-graphics" }
material-icons-extended         = { group = "androidx.compose.material", name = "material-icons-extended", version.ref = "material-icons" }

# Lifecycle & ViewModel
lifecycle-viewmodel-compose     = { group = "androidx.lifecycle",     name = "lifecycle-viewmodel-compose", version.ref = "lifecycle" }
lifecycle-runtime-compose       = { group = "androidx.lifecycle",     name = "lifecycle-runtime-compose",   version.ref = "lifecycle" }

# Navigation
navigation-compose              = { group = "androidx.navigation",    name = "navigation-compose",          version.ref = "navigation" }

# Hilt DI
# Baseline profile generation. benchmark-macro drives a real device; profileinstaller is
# already present transitively but is named here so the release APK cannot silently lose it.
benchmark-macro-junit4          = { group = "androidx.benchmark",     name = "benchmark-macro-junit4",      version.ref = "benchmark" }
androidx-profileinstaller       = { group = "androidx.profileinstaller", name = "profileinstaller",         version = "1.4.1" }
androidx-uiautomator            = { group = "androidx.test.uiautomator", name = "uiautomator",              version = "2.3.0" }
androidx-test-ext-junit         = { group = "androidx.test.ext",      name = "junit",                       version = "1.2.1" }
hilt-android                    = { group = "com.google.dagger",      name = "hilt-android",                version.ref = "hilt" }
hilt-compiler                   = { group = "com.google.dagger",      name = "hilt-android-compiler",       version.ref = "hilt" }
hilt-navigation-compose         = { group = "androidx.hilt",          name = "hilt-navigation-compose",     version = "1.4.0" }

# Room
room-runtime                    = { group = "androidx.room",          name = "room-runtime",                version.ref = "room" }
room-ktx                        = { group = "androidx.room",          name = "room-ktx",                    version.ref = "room" }
room-compiler                   = { group = "androidx.room",          name = "room-compiler",               version.ref = "room" }
room-testing                    = { group = "androidx.room",          name = "room-testing",                version.ref = "room" }

# DataStore
datastore-preferences           = { group = "androidx.datastore",     name = "datastore-preferences",       version.ref = "datastore" }

# WorkManager
workmanager-ktx                 = { group = "androidx.work",          name = "work-runtime-ktx",            version.ref = "workmanager" }
hilt-work                       = { group = "androidx.hilt",          name = "hilt-work",                   version = "1.4.0" }
hilt-work-compiler              = { group = "androidx.hilt",          name = "hilt-compiler",               version = "1.4.0" }

# Coil (image loading). Coil 3 split network fetching out of coil-core, so the OkHttp network
# artifact is required for any remote URL (artwork prewarm); it is service-loaded at runtime.
coil-compose                    = { group = "io.coil-kt.coil3",       name = "coil-compose",                version.ref = "coil" }
coil-network-okhttp             = { group = "io.coil-kt.coil3",       name = "coil-network-okhttp",         version.ref = "coil" }
# Animated GIF / animated WebP decoding for the motion wallpaper (ImageDecoder on API 28+;
# minSdk here is 29, so the slower GifDecoder fallback is unnecessary). Registered on the
# app-wide ImageLoader in ArtworkModule.
coil-gif                        = { group = "io.coil-kt.coil3",       name = "coil-gif",                    version.ref = "coil" }

# Discord Social integration
androidx-browser                = { group = "androidx.browser",       name = "browser",                     version.ref = "browser" }

# Ktor (networking)
# The OkHttp engine, not the Android one: Ktor's Android engine aborts a request by closing its
# HttpURLConnection stream from whichever thread called cancel(). Cancelling a browse mid-download
# (a source switch, a Change Match submit, the ViewModel scope being cleared) therefore threw
# "Unbalanced enter/exit" out of Job.cancel() on the main thread and killed the app. The OkHttp
# engine cancels through Call.cancel(), which is safe from any thread.
#
# This pulls com.squareup.okhttp3:okhttp 5.3.2 (and okio 3.17.0) app-wide over the 4.12.0 that
# Coil and Retrofit previously resolved. Pinning back to 4.12.0 is not possible: Ktor 3.5.2's
# Protocol.fromOkHttp() references Protocol.HTTP_3, which only exists in OkHttp 5.
ktor-client-okhttp              = { group = "io.ktor",                name = "ktor-client-okhttp",          version.ref = "ktor" }
ktor-client-content-negotiation = { group = "io.ktor",                name = "ktor-client-content-negotiation", version.ref = "ktor" }
ktor-serialization-json         = { group = "io.ktor",                name = "ktor-serialization-kotlinx-json", version.ref = "ktor" }
ktor-client-logging             = { group = "io.ktor",                name = "ktor-client-logging",         version.ref = "ktor" }
ktor-client-mock                = { group = "io.ktor",                name = "ktor-client-mock",            version.ref = "ktor" }

# Retrofit (Steam Web API client in feature-achievements' provider/steam island). The
# kotlinx-serialization converter keeps our own DTOs on kotlinx, matching the rest of the app.
retrofit                        = { group = "com.squareup.retrofit2",  name = "retrofit",                    version.ref = "retrofit" }
okhttp-logging-interceptor      = { group = "com.squareup.okhttp3",     name = "logging-interceptor",         version.ref = "okhttp" }
retrofit-kotlinx-serialization  = { group = "com.squareup.retrofit2",  name = "converter-kotlinx-serialization", version.ref = "retrofit" }

# Kotlinx
kotlinx-coroutines-android      = { group = "org.jetbrains.kotlinx",  name = "kotlinx-coroutines-android",  version.ref = "coroutines" }
kotlinx-coroutines-swing        = { group = "org.jetbrains.kotlinx",  name = "kotlinx-coroutines-swing",    version.ref = "coroutines" }
kotlinx-serialization-json      = { group = "org.jetbrains.kotlinx",  name = "kotlinx-serialization-json",  version.ref = "serialization" }

# Timber logging
timber                          = { group = "com.jakewharton.timber",  name = "timber",                      version.ref = "timber" }

# XZ / LZMA (CHD cdlz hunk decompression)
xz                              = { group = "org.tukaani",             name = "xz",                          version.ref = "xz" }

# Accompanist
accompanist-drawablepainter     = { group = "com.google.accompanist",  name = "accompanist-drawablepainter",    version.ref = "accompanist" }

# Compose Multiplatform (desktop Theme Studio). Declared directly rather than through the
# `compose.material3` / `compose.materialIconsExtended` shorthand, which CMP deprecated.
compose-mp-material3            = { group = "org.jetbrains.compose.material3", name = "material3",              version.ref = "compose-mp-material3" }
compose-mp-icons-extended       = { group = "org.jetbrains.compose.material",  name = "material-icons-extended", version.ref = "compose-mp-icons" }
jcodec                          = { group = "org.jcodec",                    name = "jcodec",                  version.ref = "jcodec" }
jcodec-javase                   = { group = "org.jcodec",                    name = "jcodec-javase",           version.ref = "jcodec" }

# Testing
junit                           = { group = "junit",                      name = "junit",                           version.ref = "junit" }
kotlin-test-junit               = { group = "org.jetbrains.kotlin",       name = "kotlin-test-junit",               version.ref = "kotlin" }
mockk                           = { group = "io.mockk",                   name = "mockk",                           version.ref = "mockk" }
mockk-android                   = { group = "io.mockk",                   name = "mockk-android",                   version.ref = "mockk" }
kotlinx-coroutines-test         = { group = "org.jetbrains.kotlinx",      name = "kotlinx-coroutines-test",         version.ref = "coroutines" }
turbine                         = { group = "app.cash.turbine",            name = "turbine",                         version.ref = "turbine" }
robolectric                     = { group = "org.robolectric",             name = "robolectric",                     version.ref = "robolectric" }
androidx-test-core              = { group = "androidx.test",               name = "core-ktx",                        version.ref = "androidx-test-core" }
hilt-android-testing            = { group = "com.google.dagger",           name = "hilt-android-testing",            version.ref = "hilt-testing" }
work-testing                    = { group = "androidx.work",               name = "work-testing",                    version.ref = "workmanager" }

[bundles]
compose                 = ["compose-ui", "compose-ui-tooling-preview", "compose-material3", "compose-animation", "compose-foundation", "compose-graphics"]
ktor                    = ["ktor-client-okhttp", "ktor-client-content-negotiation", "ktor-serialization-json", "ktor-client-logging"]
lifecycle               = ["lifecycle-viewmodel-compose", "lifecycle-runtime-compose"]
test-unit               = ["junit", "kotlin-test-junit", "mockk", "kotlinx-coroutines-test", "turbine"]
test-android            = ["junit", "kotlin-test-junit", "mockk-android", "kotlinx-coroutines-test", "turbine", "robolectric", "androidx-test-core"]

[plugins]
android-application     = { id = "com.android.application",                version.ref = "agp" }
android-library         = { id = "com.android.library",                     version.ref = "agp" }
android-test            = { id = "com.android.test",                        version.ref = "agp" }
# Tracks androidx.benchmark, NOT AGP: the Baseline Profile plugin ships on the benchmark
# release train, so its version must match the benchmark-macro-junit4 dependency below.
baselineprofile         = { id = "androidx.baselineprofile",                 version.ref = "benchmark" }
kotlin-jvm              = { id = "org.jetbrains.kotlin.jvm",                 version.ref = "kotlin" }
kotlin-compose          = { id = "org.jetbrains.kotlin.plugin.compose",     version.ref = "kotlin" }
kotlin-serialization    = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
ksp                     = { id = "com.google.devtools.ksp",                  version.ref = "ksp" }
hilt                    = { id = "com.google.dagger.hilt.android",           version.ref = "hilt" }
jetbrains-compose       = { id = "org.jetbrains.compose",                    version.ref = "compose-multiplatform" }
