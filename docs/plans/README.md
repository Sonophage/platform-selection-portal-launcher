# Plan Index

Plans generated from `docs/feedback/` plus directly requested feature work. Each plan is
independent and reviewable on its own.

**This index is the record.** Plans that have shipped are deleted once implemented — their
Status cell below carries the full account of what landed, and the git log carries the
implementing commits. Only rows still marked ❌ have a plan file left to open; rows without a
link have shipped and their file is gone (recoverable from git history if ever needed).

Status key: ✅ implemented (plan file deleted) · 🟡 partially implemented · ❌ not implemented

## Source: architecture-review-20260820-013228.html

| # | Plan | Status | Review verdict | Effort |
|---|---|---|---|---|
| A1 | Deep library scan module | ✅ `0aee279` extracts `LibraryScanner` (`scanPlatform` / `scanAllEnabled`); both callers delegate; ADR 0001; `LibraryScannerTest` + `LibraryRescanCoordinatorTest` cover the guards | Strong (top recommendation) | M |
| A2 | [Split PfpThemeStore into bundle + applied-look](theme-module-depth-plan.md) | ❌ `PfpThemeStore` untouched; the 2026-08-14/20 theme commits (wave-only bundles, hardening) are the prior work this plan builds on | Worth exploring | L |
| A3 | One rescan-trigger module behind thin Android adapters | ✅ `RescanTrigger`/`RescanTriggerBus` (feature-library) own throttle/debounce/single-flight with a DI-provided scope (`@RescanApplicationScope`); `LibraryRescanCoordinator` is a thin delegator. Clock seam (`RescanClock`) makes the resume-throttle boundary test-driven; `RescanTriggerBusTest` covers debounce collapse, in-flight single-flight, unplug-vs-resume, exception resilience, throttle boundary, and scope cancellation | Worth exploring | S |
| A4 | [Library projection out of XMBViewModel](library-projection-module-plan.md) | ❌ `XMBViewModel` untouched; the multi-disc projection landed at the DAO-query level instead (the alternative C1 explicitly allowed) | Speculative (deferred) | L |

Dependency order: A1 -> A3 -> A4. A2 is independent.

## Source: frontend-user-research.md

| # | Plan | Status | Research theme | Effort |
|---|---|---|---|---|
| B1 | Launch reliability and recovery | ✅ Missing-ROM half (detection/tracking, Missing bucket, rescan on resume/mount/unplug, launch refusal) plus the post-launch half: `launch_outcomes` table (schema v41 + `Migration40To41Test`), a single `LaunchDispatcher` every game-path launch funnels through (named startActivity failures, preflight for revoked SAF grants and dropped COMPONENT activities, home-launcher foreground verification with a 6 s stop-window / 10 s min-session), a recovery sheet (retry / change emulator / per-system defaults / copy diagnostics), and Game Detail's "Get help" affordance | Complaint #1, deal-breaker #1 | L |
| B2 | [Scraper throughput and honest failure reporting](scraper-reliability-plan.md) | ❌ | Complaint #2, deal-breaker #2 | M |
| B3 | Setup and onboarding friction | ✅ First-run wizard (re-runnable, flows match Settings, media roots scan) plus the derived half: `SetupState`/`SetupStateProvider` (feature-launcher) names the FIRST unmet step (ROM root → console card → emulator) from the live stores, XMB empty states render that gap and deep-link to the fixing screen, wizard FINISH gains "Go to your library" landing on All Games with the first playable game under the cursor, and the ROM-roots page offers "Create Standard Folders" (one ES-DE subfolder per console) | Complaint #3, deal-breaker #4 | M |
| B4 | Emulator and core assignment clarity | ✅ Typed `ResolvedLaunch(profile, core, source)` ladder extracted into feature-launcher's `EmulatorLaunchResolver` (precedence pinned by tests), shared `EmulatorPlatformMapping` replaces three private copies, Game Detail's Emulator row shows emulator + core + attributed source with a tappable change flow, Per-System Defaults screen (counts, CORE MISSING/NO EMULATOR badges, scoped bulk override clear), XMB L2 wiring | Complaint #5, must-have | M |
| B5 | [OS and emulator update resilience](os-update-resilience-plan.md) | ❌ | Complaint #4, deal-breaker #3 | M |
| B6 | [Surface the differentiators](differentiator-surfacing-plan.md) | ❌ | Deal-breaker #5, adoption gate | S |
| B7 | [Niche request triage](niche-backlog-triage-plan.md) | ❌ | Section 3 (low priority) | S |

## Source: direct request

| # | Plan | Status | Effort |
|---|---|---|---|
| C9 | UI audio, boot sequence, and GameBoot customization | ✅ Implementation handoff, implemented on `more-customization`. Phase 1 — `UiMediaLimits` (theme-kit, per-slot duration/byte caps, duration mandatory), `UiMediaSlot` (core-domain), `UiMediaStore` (`filesDir/ui-media/<slot>.<ext>`, staged import so a rejected pick cannot disturb a working assignment, `ui_media_stamp` invalidation), `MediaDisplayNames.queryDisplayName`, and both riding backup. Phase 2 — Interface ▸ Audio: `MenuSoundPlayer` resolves a custom sample over its bundled `R.raw` default with NO call-site changes (~90 sites untouched) and now observes `sound_menu_enabled` itself; new `AudioSettingsScreen`/`AudioSettingsViewModel` with per-row Preview (plays through mute) and Use Default; the old Display ▸ Sound row removed. Phase 3 — Boot Sequence: `display_show_boot`/`display_boot_on_resume` finally read (they were dead), optional custom boot video + audio as two independent one-shot players with audio enabled, and a three-layer bound (import cap, pipeline clip, 12 s watchdog) plus a Confirm/Back skip and exactly-once completion. Phase 4 — GameBoot: `GameBootGate` awaited once inside `LaunchDispatcher.launch()` after every preflight, 7 s timeout that PROCEEDS with the launch, `GameBootOverlay` with its own ExoPlayer so muting menu sounds cannot silence it, and launch-sound suppression at the two confirm sites. Deviations: media is copied into `filesDir` rather than stored as content URIs, and the setting keeps its "Menu Sounds" enable polarity instead of migrating to "Mute Menu SFX". Sound-set audit during implementation: `sfx_favorite.wav` was really a notification chime and `sfx_back.wav` really the error chime — both renamed and given their own events (`NOTIFICATION`, `ERROR`), which un-blocked the previously deferred Error sound; `sfx_quicksysselect.wav` was a byte-identical third copy of `sfx_scroll.wav` and was deleted; Favorite is now silent by decision (event and slot removed). Phase 4 follow-up (C13): the GameBoot seam gained a real default and was collapsed to ONE thing — `GameBootSequence`, a Compose-drawn PSP light sweep beat-matched to the full 5.000 s of `sfx_launch` via a measured-loudness table (motion-budget aware), behind a plain on/off boolean with a read-time migration (new installs on, established installs off) and a single replaceable `GAMEBOOT_VIDEO` slot; the three-way mode and the separate `GAMEBOOT_AUDIO` slot were retired, the audio is gate-owned and fire-and-forget through the generalized `UiMediaAudioPlayer`, and the overlay/gate watchdogs are per-path (6.5 s sequence, 11.5 s clip) behind a 13 s gate | L |
| C10 | Seven customizable sounds | ✅ Implementation handoff, implemented on `more-customization`. Replaced every Sony-derived `res/raw` sample with the `assets/SFX/active/` set (byte-identical copies; old files deleted; all seven admission scores < 0.30 recorded in `assets/SFX/active/README.md` and `REFERENCE.md`). **Deviation reversed (owner decision 2026-09-08): boot audio had shipped Sony's `snd_opening` rip (`sfx_opening.mp3`, xcorr 1.000) by explicit owner decision 2026-09-07 — the one deliberate exception to the admission rule — and ships the authored `sfx_opening.wav` (xcorr 0.088) again since 2026-09-08; the rip survives in git history only.** Roster collapsed to seven rows on Interface ▸ Sound: `SCROLL`/`SELECT`/`SYSTEM_BROWSE` merge into one Navigation row (`sound_scroll` storage key survives — no migration), `SOUND_SELECT`/`SOUND_SYSTEM_BROWSE` removed from the enum with their leftover files and prefs swept by `UiMediaStore.pruneOrphans()` (cold start + after backup restore). Boot Sound is the seventh row, shipping `sfx_opening` as its bundled default (`bundledDefaultUri`), resolved only when there is no custom boot video so a custom clip keeps its own audio track. Screen renamed Audio → Sound (`settings_audio` id kept for cursor restore). The three dead events now fire: `CONFIRM` (app/game picker commits, icon imports, save-theme, reset), `ERROR` (refused launches, rejected imports), `NOTIFICATION` (backup/restore, library rescan). Drift pins in `UiMediaDefaultsTest` / `UiMediaSlotTest` / `AudioSettingsViewModelTest` / `UiMediaStoreTest` | L |
| C7 | Custom XMB icons (static + animated GIF) with Save-as-Theme | ✅ Implementation handoff, implemented on `more-customization`. Two-phase feature: Phase 1 — per-slot icon replacement (55 theme slots + `sysicon_*` console slots) via a live fullscreen editor over the XMB (`CustomIconsOverlay`, mirrors Adjust XMB Layout plumbing: session state, gamepad routing, three-list overlay registration), `CustomIconStore` (`filesDir/custom-icons/<slot>.<ext>`, MIME gate + 8 MB cap + `CustomIconLimits`, Coil eviction via the `CustomIconCacheEvictor` DI seam), only the focused row/column animates (`LocalIconAnimating` provided per row/column, gated on battery saver + `hasBlockingOverlay`). Phase 2 — schema v3 `.pfptheme` (additive: gif icons, `sysicons/`, `motion.*`, raised limits; v2 bundles still read unchanged), `PfpThemeStore.saveCurrentLook()` flattening user-pick-over-theme-icon exactly as the render tiers draw (deliberately excludes the device-specific `XmbLayoutAdjust`), `apply()` writes gif/sysicon/motion entries, `custom-icons/` + the stamp pref ride backup, Studio adapted to `ThemeImage` bundle typing. Follow-up landed on `more-customization` — the Studio now preserves animated GIF icons: raw bytes ship as `icons/<key>.gif` (previously every import was flattened to frame 1 and labeled png), gated by the shared `IconGifSupport` probe in theme-kit (frame count, duration, 512px cap — the same numbers core-ui's `CustomIconLimits` aliases) | L |
| C8 | Theme Studio — author motion wallpapers (video pass-through) | ✅ Implementation handoff, implemented on `more-customization`. T1 `:studio` compiles (JCodec resolves; the reported PKIX failure was a stale Gradle daemon — `--stop` fixed it, no code change, and the written JCodec API surface type-checked as-is). T2/T3 — `StudioState.motionFile` scratch-file lifecycle (temp copy, deleted on replace/clear/New/open; `importVideo` → `VideoCodecs.accept` gate → frame-1 poster through the ordinary crop flow; cancel leaves neither still nor motion). T4/T5 — `exportTo` writes `motion.<ext>` via `MotionLimits.bundleExtensionFor` and never without a still; opening goes through `PfpThemeCodec.read(File)` (no 64 MB byte cap) and spills the entry to scratch so re-export preserves it byte-for-byte. T6 — inspector Motion row with poster-frozen hint, and wallpaper-picker routing parity with the launcher: a video picked at either pick point enters the motion gate instead of dying as "not a readable image" (Windows FileDialog filters are a no-op, so the pick dialog honestly lists image-or-video and the ViewModel dispatches). T7 — `MotionLimitsTest` extension↔`MOTION_EXTENSIONS` drift pins, `VideoCodecsTest` rejection ladder + real-JCodec accept, `ViewModelMotionRoundTripTest` round-trips (incl. a 40 MB entry) | M |
| C5 | [XMB icon legibility — PSP-style two-layer glyphs](xmb-icon-legibility-plan.md) | ❌ Implementation handoff, not yet started. Adds a five-option `Icon Legibility` setting (None / Offset Shadow / Contour Dark / Light / Auto) drawn in `PortalIcon`, porting the PSP's second-silhouette backdrop instead of the current 12f blur halo. Matte color derives from the theme `iconColor`'s luminance — a hardcoded dark matte is useless behind the current dark-red glyphs. Ships defaulted to `None` | S–M |
| C4 | [Animated backgrounds — user-supplied motion wallpapers](animated-backgrounds-plan.md) | ❌ Plan only, not yet started. Adds a looping MP4/WebM/GIF background under the same freeze pipeline as the wave — every freeze input releases the decoder outright and falls back to an import-time poster still, rather than holding a paused ExoPlayer | M |
| C3 | [Disc region scanning — SAF sibling resolution](disc-region-scanning-plan.md) | ❌ Implementation handoff, not yet started. `.cue`/`.gdi` region detection has never worked on either path — SAF throws a `SecurityException` (non-tree document URI), raw-path fails silently (lowercased filenames), and the region memo re-reads every unknown-region disc twice. Regional dumps in one folder are being merged into a single disc set as a result | S–M |
| C2 | Android library and app picker | ✅ Android Memory Card auto-created and re-addable (Add Console excludes Windows, not Android; ROM-root gate off the Android path); picker logic extracted to testable pure functions (`AppPickerLogic` + `gridMove` in core-navigation, 20 unit tests); picker rebuilt as a themed controller-first grid with pre-checked membership, diff-based Apply, and a removal confirmation | M |
| C1 | Multi-disc games as one library entry | ✅ Fully implemented: set identity (`812ef36`), reconciliation (`6e637d6`), one-primary-per-set invariant + single-row projection (`2c00f66`), plus every follow-up from multi-disc-next-session.md — disc picker on game detail, set-level Missing semantics, legacy multi-folder scan reconciliation, schema v39. Remaining items are the optional backlog (disc-count badge, set-level artwork decision, on-device E2E run) | M |
| C6 | Settings hierarchy — multi-level XMB-native Settings | ✅ Flat Settings list replaced by a two-level XMB-native hierarchy: L1 sections (Library / Media / Emulators / Interface / System) open as nested XMB items, L2 entries launch the existing settings screens. Structure pinned by `SettingsHierarchyTest`; `SETTINGS_SCREEN_ROUTES` in `SettingsNavHost` is the route allowlist. The touch navigation button's per-state behavior under the new drill level was decided during implementation (`AppDrawerButton` in `XMBShell`, gated on `isInSubItem`) | M |
| C11 | Permanent helper footers | ✅ `45b8d2c`. App Picker and App Drawer draw their controller hint bar as a real footer row below the grid instead of a floating `BottomCenter` overlay, so grid geometry no longer depends on whether the bar is showing. Three-rows-fit invariant pinned by `AppPickerThreeRowsTest` / `AppDrawerThreeRowsTest` | S |
| C12 | Motion wallpaper — no ExoPlayer for GIF/WebP | ✅ Animated GIF/WebP wallpapers render through Coil's `AnimatedImageDecoder` in a separate `AnimatedImageSurface` composable (`MotionWallpaperBackground.kt`); no code path builds a player for them, so re-entering the XMB no longer emits `UnrecognizedInputFormatException` | S |
| C13 | [GameBoot — one presentation, one switch, one replaceable clip](gameboot-default-sequence-plan.md) | ✅ `GameBootSequence` (renamed from `GameBootFlash`) draws the built-in PSP-style light sweep over the full 5.000 s of `sfx_launch`, its bloom riding a `LOUDNESS` table of the sample's measured 50 ms-window RMS so the light lands on the sound's own hits (2 050 ms, the 2 900–3 300 ms body, the 4 250 ms lift); one still frame instead of the sweeps under a frozen/reduced wave style, at the same full length (the motion budget drops motion, not duration, so the launch always waits for the whole presentation); the timeline runs off `withFrameMillis` deltas rather than a tween, so a reduced system animator scale cannot let the launch overtake it. The three-way `GameBootMode` and the `GAMEBOOT_AUDIO` slot were both retired in favour of ONE boolean (`display_gameboot_enabled`) and ONE replaceable slot (`GAMEBOOT_VIDEO`): a custom clip takes over the whole presentation with its own audio, `resolveGameBootAudio` is down to two branches, and `UiMediaLimits.GAMEBOOT` (the dead audio spec) is gone. Read-time migration covers both the retired mode key (anything but `OFF` reads as on) and the old boolean; `setGameBootEnabled` retires the mode key on write so a stale value cannot outrank a toggle. Launch-sfx suppression at both confirm sites keys on the boolean; overlay/gate watchdogs stay 6.5 s / 8 s; Display ▸ GameBoot is a toggle with its Video / Preview / Reset rows shown only while it is on. A user clip may run to `GAMEBOOT_CLIP_MAX_MS` (10 s, matching the boot clip) while the built-in sequence keeps `GAMEBOOT_SEQUENCE_MS` (5 s); the overlay picks its watchdog per path (6.5 s sequence / 11.5 s clip) and the gate's is 13 s. GameBoot reads the motion budget (`gameBootWaveStyle`), never the wave's `effectiveWaveStyle` — that value is frozen whenever an opaque layer covers the wave, and during a launch that layer is GameBoot itself. Plan file kept (not deleted) | M |
| C14 | [Adaptive text legibility and user-selectable font color](text-legibility-font-color-plan.md) | ❌ Plan only, not yet started. Settings ▸ Sound was measured on device: white labels degrade 6.17:1 → 1.82:1 down one screen and every accent-blue value reads 1.05–1.84:1. Two root causes — the backdrop luminance sweeps 0.091 → 0.539 so no single fill color passes top and bottom, and `PfpPalette.Accent` (#4A90D9, luminance 0.264) is structurally incapable of carrying body text (ceiling 3.34:1 on white), which no drop shadow can fix. The bright band is mostly the app's OWN paint — `SettingsScaffold` draws `backgroundBottom` at alpha 0.90 over `lighten(wave, 0.28)` — so Phase 1 fixes the measured screenshot in two files with no wallpaper sampling. Promotes the existing tested WCAG engine out of `StorefrontColors` (`relativeLuminance`/`contrastRatio`/`ensureReadable`), re-points ~20 module-level color constants at a `LocalPfpTextColors` CompositionLocal rather than adding a `PfpText` wrapper across ~400 call sites, retires accent as a text fill, adds an HSL lightness clamp with a dismissible/opt-out/suppressible notice, and gives the XMB a solved-alpha text-shaped plate fed by a precomputed wallpaper luminance map. Per-role thresholds (BODY 4.5 / LARGE 3.0) because the measured strings are 13–15sp and do not qualify as WCAG large text | L |
| C15 | [Touch controls — dead scroll zones, and backing out with LEFT](touch-controls-plan.md) | ❌ Implementation handoff, not yet started. Two gaps on the Settings/Wizard surfaces. (1) Dragging on a header does not scroll, because `SettingsScaffold` draws header/divider/footer as siblings *outside* the scrolling body (`SettingsScaffold.kt:739-872`) and `WizardScaffold` inherits the shape — aggravated by the root `Box` consuming every moved pointer change (`:700-704`) when it only wants a "touch was used" signal, which the repo already has a non-consuming form of (`GameDetailScreen.kt:226`). Fixed by a shared `Modifier.dragToScroll(ScrollableState)` in core-ui feeding the `ScrollState` the scaffold *already* holds via `LocalSettingsScrollStateRegistrar` — not by moving ~22 screens' headers inside their scroll containers, and not by the repo's first `nestedScroll` plumbing. (2) D-pad LEFT and a leftward swipe now back out of a flyout/drill-in. Both directions are documented no-ops today (`XMBViewModel.kt:4752` `cancelRepeat(); return`; `stepCategory` early-returns on `isInSubItem`; `moveHorizontal(-1)` returns null and is swallowed by a `?.let` at `SettingsScaffold.kt:618`), so nothing is taken away. LEFT is a *fallthrough*, never an override — slider adjust and `onInterceptAction` consume it earlier — and is gated on a new `controller_left_backs_out` pref (default ON, rides backup). Swipe-back is a branch in the existing `xmbNavGestures` detector mirroring its `fromEdge` commit-on-release, not a second detector racing it. Also de-duplicates the twelve-branch back-out ladder, currently written out identically at `XMBViewModel.kt:4768` and `:6387` | M |
| C16 | [Artwork Manager hardening](artwork-manager-hardening-plan.md) | ✅ **Closed 2026-09-16 (user decision): working as intended; future bugs come from user reports.** C17 deferred, `4.3` and `6.4` closed, `4.4` covered by 5.2; only `7.1` remains, blocked on B2. History: Merges 1–3 landed (`0677011`, `49ae052`): Phases 0–3 plus task 5.0. Task 2.4 landed after them: Change Match / Forget Match shipped touch-only and are now on the Triangle menu. Alongside it: every Studio source listed on every tab (image providers disabled on ICON1/Manual/Video); the XMB cursor follows a renamed game by id instead of staying on its old slot; and per-URI `ArtworkRevisions` so art rewritten at a stable portable URI reloads on screen. Layout rework task `L.1` landed (uncommitted, Studio unit tests green): a pure `StudioGridCapacity` with a per-tab tile class sets `gridColumns` × `gridRows` from the measured grid slot, re-paging keeps the focused result, and the D-pad moves by the measured column count; then `L.2` deleted the fixed 4×5 for real (`4f162ed`): the grid slot measures itself, a page is exactly one measured gridful with `userScrollEnabled = false`, and no hardcoded `20` is left in the paging pills. `L.3` (uncommitted) replaced the shared breadcrumb with a 36 dp Studio-local header row — back, title, "Artwork Studio · platform", and a trailing ≤300 dp query field a Square press or tap opens — and the tab row with eleven 24 dp flat chips between binding-aware LB/RB glyphs. In the same session the artwork HTTP client moved from Ktor's Android engine to OkHttp: cancelling a browse mid-download threw out of `Job.cancel()` on the main thread through the Android engine's connection close, a device crash no mocked-network test could see. That change lifts OkHttp to 5.3.2 app-wide (Coil and Retrofit with it), with `logging-interceptor` pinned alongside, and it still needs one on-device run to confirm cancellation is gone and image loading still behaves. `L.4` (also uncommitted) then turned the Current panel into the mockup's narrow rail — 150 dp, widening to 200 dp once the window is 1000 dp across — with the tab's contract caption moved in from under the tabs, the thumbnail drawn at the tab's tile aspect instead of a fixed 150 dp box, and a Y hint row whose glyph comes from the live bindings. `L.3` and `L.4` were committed in `a9e0d28`. `L.5` (`f25e065`) finished the mockup's lower half. Source chips now match the tabs, and SteamGridDB's NSFW checkbox became a binding-aware START · Mature badge. The match row is one 22 dp line. A 16 dp page line under the grid carries the range and LB ‹ Page x / y › RB, replacing the rail's PREV/NEXT pills, and the prompt bar dropped paging and mature. The match and page bands keep their height when empty, so the measured grid slot never resizes mid-load. `L.6` (fixes in `f25e065`, closed by the user 2026-09-11): on the Thor, capacity matched the plan's table for every tile class checked. Fixes from its screenshots: full-height FORGET / CHANGE MATCH, a query-field gap, a match line whose right edge moved with the title's length, and a theme-inherited 24 sp line height that spaced the rail caption and misaligned the page line. Pulled forward from task 4.3 at the user's request: touch-mode "‹ Prev" / "Next ›" and "⋯ Options" pills (`StudioTouchControls.kt`). `ArtworkStudioScreen` became a wrapper around a stateless `ArtworkStudioContent` driven by a new `ArtworkStudioActions` interface the ViewModel implements, so `ArtworkStudioPreview.kt` can preview the Thor, a 20:9 phone and a 16:10 tablet. Phone (914 × 411 dp) and tablet (1280 × 800 dp) were then checked with `wm size` overrides: capacity is within one column of the table, and tablet BOX ART is exactly 8 × 3. The one clipping found, the rail's touch Options pill on a short screen, is fixed and was checked on a build, which closed `L.6`. `L.6` verified the Studio layout rework across screen sizes against the approved target mockup `docs/mockups/artwork_studio_layout.html` (flat tabs; measured on the AYN Thor at 833 × 468 dp, where the grid had 87 dp of height and showed one row of a 20-result page). Chrome stays fixed in dp and the grid's capacity is measured, so larger and longer screens get more results per page; worked examples are in the plan. The matching-latency tasks `M.0`–`M.6` (user request, 2026-09-11) landed in `75243b9`: timing logs first, then one cancellable job per browse that resolves before it browses, SteamGridDB browsing by the match, no ScreenScraper title search on Windows, one ScreenScraper identity lookup instead of two, start-to-start request spacing, title searches kept between opens, and background resolution of SteamGridDB and IGDB. **Merge 4's first half** then landed. `5.1` (`fa8c8fc`, `bbc967e`) gave `StudioArt` a provider asset id and keyed selection on `kind + provider + (providerAssetId ?: url)` so a pick survives paging, a re-page, a source switch and a new query, with SteamGridDB's `grids:1` and `heroes:1` counting as two assets and ScreenScraper URLs compared by `jeuid:media` so credentials never enter the comparison. `5.2` (`8569f33`) shipped wider than planned: a multi-asset tab is now a checklist rather than a pick list — stored assets start checked, A unchecks one for removal or picks a new one, and one confirmed Apply deletes the unchecked positions highest-first (each delete closes its gap) before queueing the picks through a single sequential drain job over `studioAppendFromUrl`, one download at a time, a failure being a failed item and never a stopped queue, with Retry/Remove Failed and a leave prompt (Apply and Close / Discard Changes / Stay). START stopped toggling SteamGridDB's mature filter, which is menu-only now. Three device findings landed with it: ScreenScraper tiles were doubled because `jeuInfos` lists some files several times and the cache stores the list as served (one tile per asset now, labelled with every region it was listed under); assets added on an earlier visit were not marked, so `StudioLibraryAssets` re-reads the slot's own records and marks held tiles ADDED and unpickable; and a removal could let a later add delete another screenshot's file, because portable multi-asset names came from the position while `deleteAtAndCompact` renumbers positions without renaming files — `persistPortable` now names a new position with `ArtworkFileNaming.nextOrdinal` and a rewrite keeps its file's name, so ordinals stay ascending with position (with gaps) and Relink rebuilds the same order. `5.3` and `5.4` then landed (unit tests green 2026-09-15, device check outstanding), respecified against the tree that day — `5.3` shrinks to the single-art tabs, since 5.2 already detects held multi-asset assets, and drops its `findByChecksum` leg because nothing in the repository ever writes `artwork_records.checksum`; `5.4` is really a stored-assets manager (the Studio has no surface listing a slot's assets) and its “storage warning” is count-based against the 100-asset cap, which `nextSortOrder` currently overwrites rather than refuses, there being no free-space API in the repository and no size on a pick before it downloads. `5.4`'s reorder stays row-only: Relink derives position from the filename on purpose — the same folder-as-source-of-truth property the Windows PC export (`c9599a8`) relies on, where a `.pfpgame` records each asset's `portableName` and re-import claims its records back by exact name — so the manager must not rename files to express an order, and a Relink restoring file order is documented rather than fought. `5.3` shipped with a two-row prompt rather than the specced three: View Existing was dropped during implementation because A has already opened the candidate preview by the time Apply is pressed, so the row offered to show the image already on screen. It also writes `provider_asset_id` on single-art applies, or its own comparison would stay URL-only. `5.4`'s manager is a full-screen overlay, so L.2's measured grid slot is untouched, it reorders rows only, and Apply now refuses over the 100-asset cap before the confirmation opens. Next: crop, starting at `6.1`, now specced under Merge 5 with kind defaults only — the platform and region tiers are built and tested but ship empty, because a wrong ratio is a visible bad crop on every game of that platform and adding a row later is a data edit (AD-11). Phase 4 no longer waits on C17, which was deferred 2026-09-16: `4.3` is reduced to touch-sized targets and `4.4` is covered by 5.2's leave prompt; 7.1 is blocked on B2. Corrects an external design spec against the tree: the spec's fourteen factual claims all verify, but it misses that multi-media is blocked at *two* layers — the unique `(game_id, artwork_type)` index **and** `ArtworkFileNaming.fixedName(kind)` returning one constant filename per kind, with `saveVersionedFromUrl` pruning siblings before every write — so ordinal naming lands before the migration or screenshot #2 deletes #1. Also missed: `ICON1`/`MANUAL`/`TITLESCREEN` have no stated selection model, and no provider supports server-side paging (IGDB hardcodes `limit 1;`, TGDB returns one game, SGDB's `getArt` takes no `page`/`limit`), which both kills the spec's server-paging branch and leaves match Tiers 4–6 with no data source. Scoped down accordingly: page == one gridful (not a literal 50), identity Tiers 1–3 ship with Change Match via `SteamGridDbApi.searchGame` while the ranked suggestion picker defers, and crop ships a starter profile set behind an Original Image fallback instead of all ~40 platforms. Reuses `GameDao.kt:355-365` (reversed-COALESCE) for Fill Missing Only, `SsMediaCatalog` for the ScreenScraper cache, `TouchNavButtonMode` for controller-vs-touch, and C15's `Modifier.dragToScroll`; consumes B2's `ScrapeFailure` taxonomy rather than inventing a second one. Windows storefront identity is captured at import **and** backfilled from `launch_intent_uri`, so existing installs need no re-scan. Phases 0+1 shipped: `ArtworkFileNaming` gained a two-digit `_NN` ordinal applied to both the internal fixed name and the portable name (position 0 keeps the historic bare name, so no existing file moves, and ordinals are only PARSED for `SCREENSHOT`/`VIDEO` so a ROM stem ending in `_07` is never misread); `isPruneCandidate` is position-aware, so saving screenshot #2 can no longer delete #1. Migration 41→42 adds `sort_order`/`provider_asset_id`/`crop_profile_key` and rebuilds the unique index on the triple; 42→43 adds the `(storefront, storefront_game_id)` pair — declared on `GameEntity`, not only created in SQL, or Room’s post-migration validation rejects it exactly as `index_games_one_disc_primary` once did — and backfills it in place from `launch_intent_uri` via a pure-Kotlin `StorefrontIdentity` parser (GameHub’s `localGameId` is deliberately never read as a store id). `PcGameScanner`/`PcShortcutImporter` capture the same identity at import, fill-only. Relink rebuilds every position of a multi-asset kind, keyed on (game, type, sort_order), matching the full stem first and only falling back to an ordinal-stripped base. Phase 1 replaced the shared mutable `allResults` with a per-key LRU behind an immutable `StudioRequestKey` + monotonic generation token and an explicit reducer boundary (`showPage`), so a superseded response is dropped rather than repainting the grid — covered by a direct repro test. The query is state now (editable, submit-only, never renames the game, Reset to title), SteamGridDB’s mature filter is scoped to its own cache key and moved off Square onto that source’s context menu, Square opens search, and paging reports a real 1-based range instead of a hardcoded ×20. Note: no `42.json` was exported (Room only exports the current version and 41→42→43 landed together), so `Migration41To42Test` runs both and validates at 43. **Merge 2** (5.0, 2.1–2.3): the Game Detail media strip reads `findAll`, so extra screenshots and videos are visible. A tiered `GameMatcher` checks a saved provider id → ROM CRC32 / storefront pair → unique exact normalized title, driven by a `ProviderCapabilities` table and never crossing ids between providers. The Studio's "Matched as …" row has Change Match / Forget Match, which write exactly one provider column through `GameDao.updateProviderMatch` and touch no artwork or metadata. SteamGridDB gained `/games/steam/{appid}` as the Tier-2 storefront consumer. **Merge 3** (3.1–3.2): `MetadataRepository.fetchCandidates` is the write-free top half of `fetchForGame`, split at its existing "nothing found" return. Game Detail ▸ Update Metadata previews Current vs Incoming under Replace All / Fill Missing Only / Choose Fields / Keep Current. One `MetadataApply.plan` decides both the preview's markers and the SQL, and `updateMetadataIfMissing` grew four columns. On-device fixes landed in the same merge. AD-4 was overtaken: IGDB, TheGamesDB and ScreenScraper (`jeuRecherche`) all gained multi-result title search and by-id browsing, so every provider backs Change Match. TheGamesDB finally has a Settings key field; keyless providers are shown disabled rather than hidden; the Change Match picker was rebuilt so the IME no longer eats pad input; and a browse cancelled by a source switch is no longer cached as "No results". Task `6.5` then centralized the artwork dimension policy: a new `ArtworkDimensions` beside `CropProfiles` carries the supplied Artwork Dimension & Aspect Ratio Policy's box-front canvas for all 42 seeded platforms plus a `boxArtAspect` that prefers real source dimensions, and `boxArtAspectFor` became one line over it. Vita splits from PSP (0.59 to 0.78), the ~25 platforms that fell to a generic 0.70 now carry their own ratio, and the generic itself is 0.72. The tree's alias ids survive the move as a canonicalizing map. Row presence is exposed as `hasBoxArtPreset` because nine policy rows are themselves 430x600, so comparing against the generic canvas cannot distinguish a row from a fall-through, and `PlatformSeeder.DEFAULT_PLATFORMS` stopped being private so the coverage test asserts against the real seeded list. Task `6.3` then landed the per-game crop override: a fourth `override` tier on `CropProfileRegistry.resolve` that wins over every other, stored as a crop-profile key in the shipped `crop_profile_key` column and written per game + artwork type so a reorder or re-download cannot change it. Two choices ship — Platform Default and Original Image — because that is what the kind-defaults table can express without inventing a key format, and Platform Default stores null, so choosing it IS the Reset and later edits to the shared table still reach the game. A stored key must name its own kind, and an unrecognized one is ignored rather than honoured. The crop editor gained a real Ⓨ context menu to hold it (CROP OPTIONS), which cost task 6.7's live-preview switch its single press: Square opens search across the Studio and START is Apply Changes, so no button in the editor was semantically free. Per-category overrides are NOT implemented — `crop_profile_key` hangs off an artwork record and has nowhere to record a choice for a whole platform or collection. No migration after v43 | L |
| C18 | [Manual PC game export](windows-game-export-plan.md) | 🟡 X.1–X.5 and X.7 landed in `c9599a8` (unit tests green; the device walk, including the clear-data restore, checked by the user); only X.6 is left. Mirrors GameNative/Winlator export files for games added manually. An **Export Manual Games** button writes one `.pfpgame` JSON file per Add by ID game or legacy capture into `windows/import`, skipping games a launcher export file or a pin already brings back. Each file carries the launch intent, titles, storefront pair, confirmed provider ids and the name every artwork file was saved under, but no artwork. Scan Import Folder reads them after the launcher files, recreates or fill-only matches each game (intent validated through `ShortcutIntentSanitizer` and the verified-launcher check, since the file is on shared storage), and relinks by those exact names. Root cause it fixes: after a fresh install a manually added game has nothing to come back from, and relink only has fuzzy title matching. Pins are exported for artwork claims only and never created (D1, decided 2026-09-11). X.7 added a per-game Export Game in Game Detail ▸ Options and the XMB Triangle menu for every Windows game. X.6 (the scan creating entries for uninstalled games) is blocked on a device investigation | M |
| C17 | [Artwork Studio — spatial navigation on the shared navigation core](artwork-studio-navigation-plan.md) | ⏸️ **Deferred 2026-09-16 (user decision).** Every control it targets already has a controller path (Square, LB/RB, the Triangle menu), so the rest is polish, and C16 `4.3`/`4.4` were re-scoped off it. Task 1.1 was built, tested and deleted unused; decisions, the unbuilt 1.2 design and a latent `core-navigation` edit-mode bug are recorded in `artwork-studio-navigation-session-context.md` §8–§9. Split out of C16 task 4.1. The Studio is navigated by a three-rung ladder (`enum class StudioZone { TABS, SOURCES, GRID }`, `ArtworkStudioViewModel.kt:56`) with no Compose focus in its chrome — cursor position is `zone` plus an index, and six `when (s.zone)` blocks in the ViewModel plus four `state.zone` reads in the screen branch on it. A ladder has ordering but no geometry, which is why C16 Phase 1 had to ship search as a Square-summoned modal and the mature filter as a context-menu entry: there was no rung to give them. Sizing it found that `core-navigation` already exists — 613 pure-JVM lines with 670 lines of tests, providing `NavigationNode` (stable keys, LEFT/RIGHT `children`, `onEditStart`), a modal context stack, component-owned edit mode where Back always exits edit mode before screen navigation, nearest-survivor focus recovery, and `gridMove` for no-wrap 2D tile math — that `feature-settings` is already an adapter onto, and that `feature-xmb` already depends on and already uses in `AppPickerLogic`. So this is an adapter (`StudioNavigationState`), not a new focus system: the results grid becomes ONE edit-mode node over `gridMove` rather than twenty nodes in a model whose geometry is a single Y float per key, and the six hand-rolled overlay early-returns (the Change Match picker added one in C16 Merge 3) collapse onto the engine’s modal stack. Search, paging and C16's match row, whose FORGET / CHANGE MATCH shipped touch-only and are bridged onto the Triangle menu by C16 task 2.4, become real focusable regions, the prompt bar is derived from the focused node instead of three hand-maintained per-zone lists (which is how a stale “NSFW” label survived C16 task 1.3 rebinding that button to search — corrected in place, structure unchanged), and `StudioZone` is deleted. Deliberately does NOT promote `feature-settings`’ `internal` registration plumbing into core-ui — that would touch ~22 settings screens to prove nothing about this screen. No schema change, no new preference, no new dependency | M |
| C19 | Drawn checkmarks | 🟡 Implemented, not yet built (direct request 2026-09-13, approved against a side-by-side mockup). Every checkmark was a typed glyph (`Text("✓")`, or `☑`/`☐`), so its shape, weight and centring came from the device's fallback font and the inherited line height, and it sat off-centre in its badge or box. New `core-ui` `PfpCheck.kt` draws one path in three shapes: `PfpCheckMark` (list rows, optional 1 dp drop shadow for marks over wallpaper), `PfpCheckbox` (filled rounded square, or an outline when unchecked) and `PfpCheckBadge` (round tile badge). Swapped in at App Picker's tile badge, the wizard checkbox row, Update Metadata's field toggles, the PSP context menu, the collection and emulator pickers, the music track picker, the emulator profile editor, Backup's "What's Included" rows and the Artwork Studio match line. Left alone: `GamePickerScreen` and `LocalSteamConvertPickerDialog` (already Material `Checkbox`), and the "Invited ✓" subtitle string in the lobby invite list, which is text inside a string. Verification: a build, then one look at App Picker, the wizard's finish page and a context menu on device | S |
| C20 | Biblically Accurate PSP XMB | 🟡 Implemented, not yet built (direct request 2026-09-13). Display's "Scale & Layout" group is now **XMB Layout**, and a new row under Adjust XMB Layout applies the existing `XmbLayoutPreset.computeForWindowDp` (the Thor-tuned PSP proportions) to the current screen-size bucket after a hand-built confirmation panel: LEFT/RIGHT between Cancel and Apply, SELECT activates, BACK cancels, opening on Cancel. It is driven through the screen's `onInterceptAction`, because an `AlertDialog` window takes key events before the pad layer. "Applied" is **derived, not stored**: `XmbLayoutPreset.matches` compares the saved layout for the bucket with the preset for the current window, within 0.001 per axis (one editor step is 0.01 or 0.02). So the row greys out after Apply or after the wizard's auto-fit, and any change saved from the editor, a reset to default included, re-enables it; saving the editor unchanged keeps it grey. The bucket and preset logic moved into `feature-settings` `PspXmbLayout`, shared with the wizard's `writeAutoFitPreset`, so the two can never write different layouts. `SettingsRow` gained `enabled` (the row stays focusable, its text fades to 40 %, and SELECT and taps do nothing). Also corrected: `XmbLayoutPreset`'s KDoc named an "Auto-fit (PSP)" overlay action that does not exist. No new preference, no backup change. Verification: `XmbLayoutPresetTest` (the prefs round trip still matches; one step, a reset or no saved layout does not), then on device: Apply greys the row, Adjust ▸ Reset ▸ Save re-enables it, and the panel is walked with the pad | S |
| C21 | [Books section](books-section-plan.md) | 🟡 Tasks 1 to 7 and 9 to 13 done 2026-09-18 and walked on the device; opening a book in a reader is the one step left, branch `feat/books-section`. A fourth media section on the shape Music, Video and Photo already share: SAF library folders, a pure extension/MIME filter, a scanner, and an intent resolver that pins to a user-chosen app. Simpler than the other three because there is no in-app reader, so the section hands an `.epub` to a reader app and stops; reading position stays that app's business. The EPUB detail that shapes the filter: SAF providers report `application/octet-stream` for `.epub` more often than the real type, so the extension fallback `PhotoFileFilter` already uses is load-bearing rather than a nicety. Task 2 is the DRY move and runs before Books exists: `MusicIntentResolver` and `VideoIntentResolver` are already near-identical, so their shared half becomes one `MediaOpenIntent` rather than a third copy. Adds Room v44 (two additive tables, `CREATE TABLE IF NOT EXISTS`, nothing existing touched), a `books_default_reader` preference that rides backup, a settings screen modelled on Photo's with Video's default-player picker, and a pinned top row that opens the reader with no document. Two decisions block Task 7: whether the built-in `books` category adopts or avoids the existing user-made custom category of that name, and where the category-bar glyph comes from, since core-ui has none. A second pass on 2026-09-18 added covers and series sorting, which are one task rather than two: both come out of the same read of the EPUB's package document, so building them separately would parse every book twice. That brings Room v45 (`series`, `series_index`, `cover_uri`), an `EpubMetadataReader` built on `core:core-archive`'s `BoundedZipReader`, and the quick/deep scan split the other media sections already carry, because reading a cover means opening every archive where the first pass only read directory cursors. `XmbSortMode.SERIES` joins the existing Sort control instead of a Series drill level. A third pass added a Series browse level beside Shelves, reversing the first pass's choice of a sort mode alone: sort shipped first, the folder was asked for after living with it, and the shared data work made it a small addition. Book covers also fill the shell's hover-background slot. Still deferred: reading progress, collections, PDF/CBZ/MOBI, in-app reading | M |
| C22 | [Remove achievements](remove-achievements-plan.md) | ❌ Planned 2026-09-19, branch `chore/remove-achievements`. Deletes achievement tracking whole: RetroAchievements, Steam, the local-Steam emulator kit, Shiba Coins, the player card. Requested directly, and sequenced before the settings, game library and game edit menu rework because all three are full of achievement UI and doing it after would mean touching them twice. It is also the whole of the APK: `assets/runtime/pfp_bridge_x64.bin` is 18.2 MB of a 19.5 MB release build and has one reference in the codebase, so the app should land near 1.5 MB with roughly 10,000 lines gone. The plan's value is the boundary it draws, because two things look like achievements and are not: there are **two ROM hashers**, and the one that stays (`feature-artwork/rom/RomHasher`) is what writes `romCrc32` on every scrape, so cutting the wrong one breaks artwork matching silently. The real entanglement is that `PcGameScanner`, part of Import PC Games which stays, imports `LocalSteamGameImporter` from the module being deleted; that importer depends on `AchievementController` and `LocalCopyOwnership` so it cannot simply move, and the decision is to drop the local-Steam contribution and keep every other PC import path. Adds Room v46 dropping six tables, the only irreversible step. `BuiltInCategory.ACHIEVEMENTS` must join `Category.RETIRED_IDS` or a restored backup resurrects the category on a build with no code to draw it, exactly as happened with Discord's `social` | M |

Effort key: S = under a day, M = a few days, L = a week or more.
